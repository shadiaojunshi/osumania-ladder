# 合包提速：现在做了哪一刀、矩阵并行值不值

> 回答 2026-09-19 的问题「有没有办法加快合包速度，比如用我的电脑辅助」。
> 结论先说：**自己的电脑不划算**（R2 出口流量虽免费但带宽一样受家用上行限制，13.8GB 全量更是走不动），
> 便宜的一刀已经落地；**矩阵并行先别做** —— 收益没有想象中大，风险集中在三处共享状态上，
> 而且这条发布链**至今没在 R2 上真跑通过一次**。

## 1. 现在的耗时结构（读代码得到的事实）

- 全量发布 = 一个 job 里 `for (const type of allTypes) await generatePack(type)` —— **串行**。
- `allTypes` 取自数据里实际出现的 `realType`，实测是 **44 个**，不是 5 个
  （RC / LN / HB / SV / TB 只是**大类**，键型目录里有 44 个具体键型）。
- 44 个键型共 **77 个包**（分包规则：≤120→1、≤200→2、≤270→3、≤360→4，之后按 90/包继续分）。
  也就是 77 次「并发下载该包的全部 `.osz` → JSZip 打包 → `PutObject` 上传」。
- 全库进包谱面 **4986 张**（`PACK_EXCLUDED_REAL_TYPES` 的 4 类已排除）≈ 你记的 "13.8GB"。
- 包内下载并发 = `DOWNLOAD_CONCURRENCY`：**原来是写死的 4**，现在默认 8、可用
  `PACK_DOWNLOAD_CONCURRENCY` 调到上限 32。

工作量分布（这张表决定了并行的天花板）：

| 键型 | 谱面 | 包数 |
|---|---|---|
| DE | 354 | 4 |
| TB | 336 | 4 |
| HB1 | 284 | 4 |
| SS / MX / HB3 / JS / SA / DP / FCJ / HB2 / TC | 204 ~ 250 | 各 3 |
| LNMX / CJ / CO / LNTC / RE / ADP | 133 ~ 188 | 各 2 |
| 其余 28 个键型 | 1 ~ 102 | 各 1 |

最大的一块 **DE 只占 7.1%**，最小的是 OLN / IN / OHB 各 1 张。

## 2. 矩阵并行能快多少

- 天花板 = 1 ÷ 最大分片占比。若按 `realType` 硬分 44 片，理论上限约 **14×**。
- 但 GitHub Actions 单仓库并发 job 数有上限（免费 / Team 是 20），44 个 job 会排队成好几波，
  每波都要重来一遍 `npm ci`（约 40~90s）+ checkout。
- **现实估计：约 4~6×**，前提是**按谱面数均衡分成 6~8 个分片**，而不是按 realType 硬分 44 片。
  依据：
  - 每个 runner 的 R2 读带宽与 zip CPU 各自独立，这两块确实能近似线性叠加 → 所以 4~6× 是站得住的；
  - `npm ci` / checkout 这类固定开销会乘以分片数；
  - 分片再细，长尾会被「最大分片 + 固定开销」顶住，不会继续变快。
- 花费基本不变：总工作量没变，多出来的只是「分片数 × 固定开销」那几分钟。

## 3. 风险：三处共享状态都要额外接一层合并

1. **manifest**：现在是一次性写 `data/packs-manifest.json` + `.previous` 再提交推送。
   矩阵下每个 job 都会写、都会 push → 必然互相打架。必须改成：分片 job **只上传 artifact**，
   最后由一个 merge job 汇总、写一次、提交一次。
2. **身份报告**：`writeIdentityReport(typeResults)` 的第三节「内容摘要相同但身份来源不同」要**跨类型**汇总。
   分片后每片只有自己那部分，得把分片的 JSON 拼起来才能出这份报告。
3. **Drive**：`upload-to-gdrive.js` 依赖 `.previous` 算孤儿，必须留在 merge 之后单跑；
   `concurrency: packs-publish` 分组也要继续和 `upload-packs-to-drive.yml` 排队。
4. **R10 门控不能丢**：现在「任一类型失败 → 整次不发布」。矩阵下必须保证
   **分片 job 一个字节都不提交**，失败时 merge job 不发布。`needs` 的默认行为是
   依赖失败就跳过 merge（方向正好对），但**要显式写死，绝不能加 `if: always()`**。
5. 最要紧的一条：**这条发布链至今没在 R2 上真跑通过一次** ——
   清单里没有 `objectKey`、R2 对象 last-modified 全是 2026-08-13。
   在一条还没验证过的链路上做并行，等于把「一次难查的失败」变成「五次同时难查的失败」。

## 4. 建议的执行顺序

1. **先用最小的键型跑一次真发布，把链路走通**：`--type=OLN --publish`（1 张图 / 1 个包）。
   验收点：R2 里出现对象、清单里出现 `objectKey`、下载页能下到新包。
2. 从这次 job log 里读**每个包的真实耗时**，拿到实际的「下载 GB/分钟」和「zip 分钟」。
3. 有了真实数字再决定分片数，并且按**均衡分片**（不是按 realType 硬分），
   同时先把第 3 节里的 1、2、4 三层合并做出来。

## 5. 已经落地的那一刀（2026-09-19）

| 文件 | 改动 |
|---|---|
| `scripts/generate-pack.js` | 写死的并发 `4` → `PACK_DOWNLOAD_CONCURRENCY`（默认 8，上限 32，非法值回退默认值）；comparator 抽成 `compareTournamentsForSources` 以便单测 |
| `.github/workflows/generate-packs.yml` | `PACK_DOWNLOAD_CONCURRENCY: '12'` |
| `.github/workflows/identity-report.yml` | 同上（同一条读路径） |

**没有动** `scripts/backfill-bid.mjs` 的下载并发（那里也写死 4）。它是一次性脚本、
下载的是整份 `.osz`，改并发只是把一次性成本从 ~1 小时压到 ~半小时，
而它马上就要跑第一次 —— 收益和「跑之前动它的风险」不成正比。要改说一声，一行的事。

## 6. 顺带记一下真正的瓶颈在哪

不是脚本本身，而是**每条记录的网络往返**：`listObjectsV2` 分页 + 逐张读对象，
单条 4~8s 的量级。所以：
- 并发旋钮是**唯一**不用改架构就能提速的地方（已落地）；
- 想再快，就只能靠「多开 runner 各自跑一段」（= 第 2 节的矩阵），
  或者把**.osz 只读尾部**（EOCD + 中央目录 + 那一个 `.osu` 条目）来减少下载量 ——
  后者对 `backfill-bid` 这种"只要元数据"的链路收益极大，是另一个独立的题目。
