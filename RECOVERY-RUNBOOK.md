# 数据回档手册（事故处置用）

写给站长与接手的人。目标场景：**数据被人恶意篡改/删除，要尽快、彻底地回到篡改之前。**

---

## 0. 结论先看（2026-09-18 核实）

| 数据 | 「回到上一次提交」 | 「回到前一天备份」 |
| --- | --- | --- |
| 比赛 JSON（`data/tournaments/*.json`）、`packs-manifest`、`references`、`ref-ladder` | ✅ **能**，而且能回到任意一次提交（截至今天 `data/` 有 **225 个提交快照**） | ✅ 能（Git 每次提交就是一份完整快照，比"前一天"更细） |
| R2 里的谱面文件 `.osz`（含包对象） | 不适用（它们不在 git 里） | ✅ **能**（2026-09-18 起）：备份在覆盖镜像之前，会把**变更前的旧内容**存进 `snapshots/<日期>/`，保留 7 天。⚠️ **快照保留期 = 你的"发现窗口"** —— 超过 7 天才发现，或攻击者手里有 R2 密钥直接把备份桶也清了，就仍然救不回 |

**所以现在最要紧的一条不是"怎么恢复"，而是"先别让备份被污染"** —— 见 §1 第 1 步。

---

## 1. 察觉异常后的头 10 分钟（顺序不能错）

1. **先停自动备份**：GitHub → Actions → **Backup R2 + Cleanup Trash** → `Disable workflow`。
   它每天北京时间约 02:07 跑一次，**跑一次就少一层保险**（镜像会覆盖同键内容）。
   同时确认 **Generate Map Packs / Upload Packs to Drive** 也停掉（它们会改 R2/manifest）。
2. **冻结写入**：在 Cloudflare 边缘拦掉 `/api` 的写请求（或临时把嫌疑账号降级）。
   这一步比等 KV 撤权传播（约 60 秒）更可靠。
3. **先别 build、别 push 任何东西**。Pages 一重建就会用最新数据；恢复没做完之前不要触发。
4. **只读取证**（都别改东西）：
   - 最后可信 commit：`git log --date=iso --oneline -- data/ | head -20`
   - 可疑 commit 的 SHA、改动文件列表：`git show --stat <SHA>`
   - 后台审计日志（`/admin` 的审计页，按 `audit:` 前缀可取全文）
   - 回收站里有哪些条目（后台「回收站」页，条目保留 30 天，带审计键）
   - **备份桶当前的对象清单**（如果还有权限）—— 这是判断"备份有没有被污染"的唯一依据
5. **判断范围**：只有几场比赛？全库？有没有 `.osz` 被覆盖或删除？分别走 §2 / §3。

---

## 2. 比赛数据（JSON）恢复 —— 这条是有保证的

### 单场被删（30 天内）
后台 → **回收站** → 核对删除人/比赛 ID/时间 → 恢复。
注意：目标文件已存在时会返回冲突并保留副本，**不会盲目覆盖**。

### 少量/多场被改坏
用你机器上已有的仓库（不用重新 clone）：

```bash
# 1) 看清要恢复的文件在哪些提交里被改过
git log --date=iso --oneline -- data/tournaments/<比赛id>.json

# 2) 从可信 commit 恢复指定文件（工作区，不碰索引）
git restore --source=<可信commit> --worktree -- data/tournaments/<比赛id>.json

# 3) 只看差异，确认恢复内容正确
git diff -- data/tournaments/<比赛id>.json
```

### 全库/大量被改
```bash
# 先看范围与规模，再决定
git diff --stat <最后可信SHA> HEAD -- data/

# 整目录恢复（注意：会把该目录其后所有合法改动一起撤掉）
git restore --source=<最后可信SHA> --worktree -- data/
```
恢复范围大时，建议先只恢复坏掉的那几个文件，或者整目录恢复之后再重放已确认的合法改动。

### 恢复之后
1. 核对：`npm test`、首页比赛数、目标轮次/谱面、下载页链接。
2. 提交并推送（**一个恢复 commit 就够**；禁止 force push / 重写历史）。
3. 等 Pages 重建完再看线上。**不要只回滚 Pages deployment** —— 那只改展示，权威数据还在 GitHub 里。

> ⚠️ 别在有未提交改动的开发目录里做 `reset --hard` / `clean`。要动历史就换一个干净的 checkout。

---

## 3. R2 谱面文件（`.osz`）恢复

按这个顺序找，**逐级降级**：

0. **变更前存档快照**（2026-09-18 起，**优先看这个**）：`<备份桶>/snapshots/<YYYY-MM-DD>/maps/…`。
   每一份快照保存的是「**那一次变化之前**」的内容，所以**从最新日期往回找该 key 的第一份快照**，
   就是它被改之前的样子。恢复方法：把那份对象复制回主桶的对应 key（先复制到临时 key 验证能解压）。
   （只在对象真的变化当天才写，所以不是每个对象每天都有。）
1. **回收站副本**：`trash/` 前缀下有删除时的可恢复副本（30 天内）。后台回收站能直接恢复。
2. **上一版归档**：`versions/` 前缀（每次**重传**会把旧对象归档到这里）。
   ⚠️ **每个槽位只留最近一版** —— 同一个槽位被连改两次，最早那版就没了。
3. **备份桶**：`<备份桶>/maps/...`（同键镜像）。
   ⚠️ **只在它没被后续备份覆盖过的情况下才是好版本**。判断方法：比对 `size` / `ETag`，或与该谱面的包清单 `sizeMB` 对照；**别整桶覆盖回去**。
4. 都不行了 → 只能让站长重新上传该谱面（数据损失局限在那一张图）。

**约束**：恢复用的 R2 凭据只站长使用；备份桶不要在 Web 管理端挂载。

---

## 4. 攻击面与「变更前存档」（2026-09-18 已补上）

**2026-09-18 已补上**：备份脚本现在在**覆盖镜像之前**先把镜像里的旧内容存档到
`snapshots/<YYYY-MM-DD>/maps/…`（保留 7 天后轮转删除）。下面是它挡住的那条攻击路径，以及
仍然存在的边界。

### 4.1 之前挡不住的攻击（现在被这道存档挡住了）

1. 攻击者用**后台的上传功能**（contributor 就够），对同一个槽位**连续重传两次**垃圾 `.osz`：
   - 第 1 次：原图被归档到 `versions/` —— 还有救；
   - 第 2 次：`versions/` 里那份**被覆盖**（`mapKeys.ts` 写明「归档键固定，每个槽位只留最近一版」）——
     这层保险没了。此时主桶里只剩垃圾。
2. 攻击者**等一晚**：次日约 02:07 的每日备份跑过。

**加存档之前**：备份发现主桶变了 → 把备份桶里的好副本**覆盖成垃圾** → 那张原图在所有地方都没有好副本了。
**加存档之后**：备份在覆盖之前先把那份好副本写进 `snapshots/<那天>/maps/…` →
**攻击反而帮我们把"被改之前的那一份"存了下来**。恢复时按 §3 第 0 步取回来即可。

### 4.2 实现方式与边界

- 只在对象**真的发生变化**的那天才多写一份存档，所以存储与 CI 时长几乎和原来一样（比"每天全量快照"便宜得多）。
- **存档失败就不覆盖镜像**：宁可让镜像暂时落后一天，也不把唯一的好副本换掉；该对象计入失败，下次运行重试。
- 快照轮转只删 `snapshots/<YYYY-MM-DD>/…` 形状的键，且**只在本次备份完整成功时**才执行删除；
  非日期形状的键（例如你手放的说明文件）一律不动。
- 保留天数由 workflow 的 `R2_SNAPSHOT_RETENTION_DAYS` 控制（当前 **7 天**）。
  **设成 `0` 就退回"纯镜像"的旧行为**（不存档、不轮转）。
- **它救不了的情况**（要知道）：
  1. **发现得太晚** —— 超过保留期，快照已被轮转删掉；
  2. **攻击者手里有 R2 密钥**（不只是后台账号）—— 他能直接把备份桶和快照一起清掉。
     那种情况下唯一还剩的是 Drive 上的包镜像（另一个账号/服务商）与本地 git。**独立灾备的价值就在这**；
  3. **`maps/` 之外的东西**：包对象本来就不备份（可由 `maps/` 重新生成），但反过来说，
     `maps/` 一旦真的丢了，"重建包"的能力也一起没了 —— 这正是存档要护住它的原因。

### 4.3 仍然建议

- 发现异常仍然**先停 Backup R2 workflow**（避免后续备份继续覆盖镜像；存档已经给了缓冲，但没必要冒险）。
- 每季度演练一次：随便改一个槽位 → 等一次备份（或手动触发）→ 从 `snapshots/` 取回旧版本验证。

## 5. 建议定期做的两件事

1. **恢复演练（每季度一次）**：
   - 挑一场比赛，随便改一个字段 → 用 §2 的命令恢复 → 确认线上重建后正常；
   - 挑一张 `.osz`，从 `versions/` 复制到临时 key，验证能解压、谱面在。
2. **确认备份真的在跑**：Actions → Backup R2 + Cleanup Trash → 看**最后一次成功运行的日期与结论**。
   备份脚本本身会在失败时非零退出并且**跳过所有清理**（不会删东西），但 workflow 存在 ≠ 备份成功。
   （本机没有 `gh`、仓库是私有的，所以这一步只能在 GitHub 网页上看。）

**独立灾备提醒**：GitHub 账号与 Cloudflare 账号同时失守时，同账号里的历史与备份不算独立备份。建议离线保存一份仓库镜像 + 关键配置说明。

---

## 6. 相关文档

- `docs/security-review-and-recovery-2026-09-18.md` —— 安全审查原文（含撤销权限、边缘阻断、R2 恢复的完整叙述与告警设计）
- `PROJECT-REVIEW-2026-09-12.md` R06/R07 —— 上传/删除的写路径语义（`versions/`、`trash/`、条件写）
- `scripts/backup-r2.js` + `.github/workflows/backup-r2.yml` —— 每日备份的实现（镜像语义写在文件头注释里）
